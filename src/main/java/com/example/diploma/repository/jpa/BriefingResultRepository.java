package com.example.diploma.repository.jpa;

import com.example.diploma.entity.BriefingResultEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BriefingResultRepository extends JpaRepository<BriefingResultEntity, Long> {
}
